# Python bytecode
__pycache__/
*.pyc

# Downloaded structures and analysis outputs (regenerated by af_study.py)
data/
results/

# results_250/ is the committed 250-protein run, kept on purpose

# Job run logs (created at submit time on the cluster)
logs/

# Registry build checkpoint (working file, delete to force a clean rebuild)
.registry_checkpoint.json

# Python venv built by setup_nixos.sh
.venv/
